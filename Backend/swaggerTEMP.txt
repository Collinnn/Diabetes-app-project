swagger: "2.0"
info:
  description: "Description"
  version: 0.0.1
  title: "Title"
host: "localhost:8080"
basePath: "/api"
schemes:
- "https"
- "http"
paths:
  /patient:
    get:
      summary: "Gets a list of patients"
      responses:
        "200": 
          description: "The list of patientsID"
          schema:
            type: "array"
            items:
              $ref: "#/definitions/Patient"
    post:
      summary: "Adds a patient to the database"
      parameters:
      - in: "body"
        name: "patient"
        required: true
        schema:
          $ref: "#/definitions/Patient"
      responses:
        "200":
          description: "Patient correctly added"
          
  /patient/{patientId}:
    delete:
      summary: "Deletes a patient"
      parameters:
      - in: "path"
        name: "patientId"
        type: "integer"
        description: "The id of the patient to delete"
        required: true
      responses:
        "204":
          description: "The patient has been deleted"
          
  
definitions:
  Patient:
    type: "object" 
    properties:
      id: 
        type: "integer"
      password:
        type: "string"
      firstName: 
        type: "string"
      lastName:
        type: "string"
      email:
        type: "string"
      doctorId:
        type: "integer"
  Doctor:
    type: "object"
    properties:
      id:
        type: "integer"
      password:
        type: "string"
      firstName:
        type: "string"
      lastName:
        type: "string"
      email:
        type: "string"
  
  #    Measurements:
  #      type: "array"
  #      items:
  #        $ref: "#/definitions/Mesurements"
  #Mesurements:
  #  type: "object"
  #  properties:
  #    id:
  #      type: "integer"
  #    name:
  #      type: "string"
        